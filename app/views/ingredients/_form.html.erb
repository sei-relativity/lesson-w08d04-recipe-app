<%= form_for @ingredient do |f| %>
    <div>
        <%= f.label :name, 'Name'%>
        <%= f.text_field :name%>
    </div>

    <%= f.submit "Submit", :class => "btn waves-effect waves-light" %>
  <% end %>